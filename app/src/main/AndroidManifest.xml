<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.d3ifcool.lop">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.TestActivity" />
        <activity android:name=".views.ResultActivity" />
        <activity android:name=".views.DescActivity" />
        <activity android:name=".views.HomeActivity" />
        <activity android:name=".views.TargetActivity" />
        <activity android:name=".views.TipsActivity" />
        <activity android:name=".views.AchievementActivity" />
        <activity android:name=".views.EditActivity" />

        <provider
            android:name=".database.LopProvider"
            android:authorities="org.d3ifcool.lop" />

    </application>

</manifest>